management:
  endpoints:
    web:
      exposure:
        include: prometheus
logging:
  level:
    root: INFO
    org.springframework.web: INFO
springdoc:
  swagger-ui:
    path: /swagger
server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true